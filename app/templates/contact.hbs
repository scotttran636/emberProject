<h1>Contact Page</h1>

<div class="jumbotron text-center">
   <h1>Coming Soon</h1>

   <br/><br/>

   <p>Don't miss our launch date, contact us now.</p>

   <div class="row">
    <div class="col-md-6">
      <div class="form-group">
          {{input type="email" value=emailAddress class="form-control" placeholder="Please type your e-mail address." autofocus="autofocus"}}
      </div>
      <div class="form-group">
          {{textarea value=message class="form-control" rows=5 placeholder="Please type your message here." autofocus="autofocus"}}
      </div>
      <div class="form-group">
          <button class="btn btn-primary btn-lg btn-block" {{action 'sendMessage'}} disabled={{isDisabled}}>Send message</button>
      </div>
      <div class="form-group">
        {{#if responseMessage}}
          <div class="alert alert-success">{{responseMessage}}</div>
        {{/if}}
      </div>
    </div>
  </div>

</div>
